const DeletePrimaryInterestRegionErrorModalContent = () => {
  return (
    <div className="flex w-full flex-col items-start gap-2">
      <p className="text-[16px] font-medium leading-[22.4px] text-blue_gray-900">
        기본 도시는 삭제할 수 없습니다. 기본 도시를 변경한 뒤 다시 시도해주세요.
      </p>
    </div>
  );
};

export default DeletePrimaryInterestRegionErrorModalContent;
